name: Publish asset package to npm

on:
  push:
    tags:
      - "asset-base-v*"
      - "snapsort-v*"
      - "snapline-v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Verify release commit is on main
        run: |
          git fetch origin main
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main

      - run: npm ci
      - run: npm run validate:packages

      - name: Resolve package from tag
        id: package
        run: |
          case "$GITHUB_REF_NAME" in
            asset-base-v*)
              package_dir="assets/asset-base"
              tag_version="${GITHUB_REF_NAME#asset-base-v}"
              ;;
            snapsort-v*)
              package_dir="assets/snapsort"
              tag_version="${GITHUB_REF_NAME#snapsort-v}"
              ;;
            snapline-v*)
              package_dir="assets/snapline"
              tag_version="${GITHUB_REF_NAME#snapline-v}"
              ;;
            *)
              echo "Unsupported release tag: $GITHUB_REF_NAME"
              exit 1
              ;;
          esac

          package_version=$(node -p "require('./$package_dir/package.json').version")
          package_name=$(node -p "require('./$package_dir/package.json').name")

          if [ "$package_version" != "$tag_version" ]; then
            echo "Tag version $tag_version does not match $package_dir/package.json version $package_version"
            exit 1
          fi

          echo "package_dir=$package_dir" >> "$GITHUB_OUTPUT"
          echo "package_name=$package_name" >> "$GITHUB_OUTPUT"
          echo "package_version=$package_version" >> "$GITHUB_OUTPUT"

      - name: Check whether version is already published
        id: registry
        run: |
          if npm view "${{ steps.package.outputs.package_name }}@${{ steps.package.outputs.package_version }}" version >/dev/null 2>&1; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
          fi

      - if: steps.registry.outputs.already_published != 'true'
        run: npm publish "./${{ steps.package.outputs.package_dir }}" --access public --provenance

      - name: Verify published registry version
        run: |
          for attempt in 1 2 3 4 5 6; do
            published_version=$(npm view "${{ steps.package.outputs.package_name }}@${{ steps.package.outputs.package_version }}" version 2>/dev/null || true)
            if [ "$published_version" = "${{ steps.package.outputs.package_version }}" ]; then
              exit 0
            fi
            sleep 10
          done
          echo "Registry did not return the expected package version"
          exit 1
